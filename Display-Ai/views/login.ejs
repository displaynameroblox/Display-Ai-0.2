<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Login</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body { background:#0b0b0b; color:#fff; font-family: Inter, system-ui, sans-serif; display:flex; align-items:center; justify-content:center; height:100vh; margin:0; }
    .card { background:#111; padding:28px; border-radius:10px; width:320px; box-shadow: 0 6px 18px rgba(0,0,0,.6); }
    input, button { width:100%; padding:10px; margin-top:8px; border-radius:8px; border:1px solid #222; background:#0f0f0f; color:#fff; }
    button { background:#f55; border:none; cursor:pointer; font-weight:600; }
    .small { color:#999; font-size:13px; margin-top:10px; }
    .error { color:#ff7b7b; margin-bottom:8px; }
  </style>
</head>
<body>
  <div class="card">
    <h2 style="margin:0 0 8px 0">Login</h2>
    <% if (typeof error !== "undefined" && error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form action="/login" method="post">
      <label>Username
        <input name="username" required />
      </label>
      <label>Password
        <input name="password" type="password" required />
      </label>
      <button type="submit">Sign in</button>
    </form>
    <div class="small">Default test user: <strong>feras</strong> / <strong>1234</strong></div>
  </div>
</body>
</html>